/** HTML comment sentinel where layout `{children}` is spliced in. */
export const CONTENT_MARKER_HTML = "<!--_ANPAN_CONTENT_-->";

/** VNode `type` that renders as {@link CONTENT_MARKER_HTML}. */
export const CONTENT_MARKER_VNODE_TYPE = "__anpan_content_marker__";
